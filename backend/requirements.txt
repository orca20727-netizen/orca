fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0
websockets>=12.0
python-dotenv>=1.0.0
httpx>=0.27.0
langdetect>=1.0.9

# ORCA Fisherman AI Decision Studio -- local ML pipeline (backend/ml/).
# Pinned to a minor-version range matching what backend/ml/models/*.pkl
# were trained/pickled with, since scikit-learn's own docs warn pickled
# models aren't guaranteed compatible across scikit-learn versions. If
# these are ever bumped, retrain (backend/ml/training/train_*.py) and
# recommit the .pkl files in the same change.
numpy>=2.4.0,<3.0.0
pandas>=3.0.0,<4.0.0
scikit-learn>=1.8.0,<1.9.0
joblib>=1.5.0,<2.0.0
